using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Direction
{
    forward, // z+ direction
    back, // -z direction
    right, // +x direction
    left, // -x direction
    up, // +y direction
    down // -y direction
}
